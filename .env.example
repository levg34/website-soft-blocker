# Example environment variables for website-soft-blocker
# Copy this file to `.env` and customize values as needed.

# Target site to apply the reminder page to (required).
# Use the full origin (protocol + host), e.g. https://example-distracting-site.test
TARGET_URL=https://example-distracting-site.test

# Application port (if you run a local server to serve the reminder page)
PORT=8080

# Runtime environment
NODE_ENV=development

# Storage mode for stats and badges: `localstorage` (default, privacy-first) or `mongodb`.
# If you keep `localstorage`, no remote DB is used and all data stays in the browser.
STORAGE_MODE=localstorage

# Optional: MongoDB connection string (used only when STORAGE_MODE=mongodb)
# Example (replace username/password/host/db):
# MONGODB_URI=mongodb://username:password@localhost:27017/website-soft-blocker
MONGODB_URI=

# Path to the badges file (relative to project root). Kept here so backend can load it.
BADGES_FILE=./badges.json

# Session secret (if a backend session is used). Change to a strong secret in production.
SESSION_SECRET=change-me

# Logging / debug level: error, warn, info, debug
LOG_LEVEL=info

# Analytics / telemetry. Default is false to preserve privacy. If you add opt-in analytics,
# set to `true` only with explicit user consent.
ENABLE_ANALYTICS=false

# Enable periodic backups of DB (only relevant if using MongoDB). false/true
BACKUP_ENABLED=false
# Local path for backups (if any server-side backup script is used)
BACKUP_PATH=./backups

# Rate limit visits/actions per minute (for optional server-side enforcement)
RATE_LIMIT_PER_MIN=60
